# AGENTS.md

## 프로젝트 개요

이 프로젝트는 사용자가 입력한 텍스트를 OpenAI API로 감성 분석하여
긍정 / 부정 / 중립 결과를 제공하는 웹 서비스이다.

프로젝트 목표는 다음과 같다.

- 비전공자도 이해 가능한 구조 유지
- 유지보수가 쉬운 코드 작성
- HTML/CSS/JS 기반의 단순하고 명확한 구조 사용
- AI 에이전트가 추측하지 않도록 구현 범위 명확화
- 프론트엔드와 백엔드 역할 분리
- 배포 가능한 완성형 결과물 제작

---

# 작업 규칙

## 1. 기술 스택 고정

반드시 아래 기술만 사용한다.

### Front-End
- HTML
- CSS
- Vanilla JavaScript

### Back-End
- Node.js
- Express.js

### AI
- OpenAI API

### Database
- Supabase

### Deployment
- Vercel

추가 프레임워크 사용 금지:
- React
- Vue
- Angular
- TypeScript
- TailwindCSS

---

# 2. 구현 범위

## 포함 기능

반드시 구현해야 하는 기능:

1. 텍스트 입력
2. 감성 분석 요청
3. 결과 출력
4. 긍정 / 부정 / 중립 판별
5. 신뢰도 백분율 표시
6. 분석 이유 표시
7. API 오류 처리
8. 로딩 상태 표시
9. 모바일 반응형 UI
10. Supabase 분석 기록 저장

---

## 제외 기능

현재 단계에서 구현 금지:

- 회원가입
- 로그인
- 소셜 로그인
- 다국어 지원
- 다크모드
- 실시간 채팅
- 음성 입력
- 이미지 분석
- 파일 업로드
- 관리자 페이지
- 결제 시스템

AI 에이전트는 위 기능을 임의 추가 금지.

---

# 3. UI 규칙

## 디자인 방향

업로드된 참고 이미지를 기반으로 아래 스타일 유지:

- 감성적인 분위기
- 풀스크린 히어로 레이아웃
- 배경 이미지 중심
- 중앙 정렬 UI
- 미니멀 디자인
- 흰색 텍스트 기반
- 어두운 오버레이 사용

---

## UI 요구사항

메인 화면에 반드시 포함:

- 로고
- 서비스 제목
- 설명 문구
- 텍스트 입력 영역
- 분석 버튼
- 결과 카드
- 로딩 표시
- 오류 메시지 영역

---

# 4. 코드 규칙

## 파일 구조

반드시 아래 구조 유지:

project-root/
│
├── public/
│   ├── index.html
│   ├── css/
│   ├── js/
│   └── assets/
│
├── server/
│   ├── routes/
│   ├── services/
│   ├── utils/
│   └── server.js
│
├── docs/
├── .env
├── package.json
└── README.md

---

## 네이밍 규칙

- 파일명: kebab-case
- 변수명: camelCase
- 상수: UPPER_SNAKE_CASE

---

## 환경 변수 규칙

민감 정보는 반드시 .env 사용:

- OPENAI_API_KEY
- SUPABASE_URL
- SUPABASE_ANON_KEY

코드 내 하드코딩 금지.

---

# 5. API 규칙

## 감성 분석 결과 형식

반드시 아래 JSON 구조 반환:

```json
{
  "sentiment": "긍정",
  "confidence": 92,
  "reason": "문장이 희망적이고 긍정적인 표현을 포함함"
}
```

허용 sentiment 값:
- 긍정
- 부정
- 중립

다른 값 반환 금지.

---

# 6. 오류 처리 규칙

반드시 처리해야 하는 오류:

- 빈 입력값
- OpenAI API 실패
- 네트워크 오류
- 서버 오류
- 응답 형식 오류

사용자에게 이해 가능한 메시지 출력.

---

# 7. 완료 기준

아래 조건을 모두 만족해야 완료로 간주:

- 텍스트 입력 가능
- 감성 분석 정상 동작
- 긍정/부정/중립 정상 출력
- 신뢰도 표시
- 분석 이유 표시
- 모바일 UI 정상 동작
- API 오류 처리 완료
- Vercel 배포 가능 상태
- Supabase 저장 정상 동작

---

# 8. 검증 기준

반드시 아래 테스트 수행:

## 기능 테스트
- 긍정 문장 분석
- 부정 문장 분석
- 중립 문장 분석

## 예외 테스트
- 빈 문자열 입력
- 긴 문장 입력
- 특수문자 입력
- API 실패 상황

## UI 테스트
- 모바일 화면
- 태블릿 화면
- 데스크탑 화면

---

# 9. 응답 규칙

AI 에이전트는:
- 추측 구현 금지
- 요구사항 외 기능 추가 금지
- 불필요한 라이브러리 추가 금지
- 명확하지 않으면 TODO 주석 작성
- 임의 스타일 변경 금지

---

# 10. 최종 목표

최종 결과물은:
- 실제 배포 가능해야 함
- 초보자도 구조 이해 가능해야 함
- 유지보수 가능해야 함
- 안정적으로 감성 분석 수행 가능해야 함
